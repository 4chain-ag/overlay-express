# ===========================================
# OverlayExpress Server Configuration
# ===========================================

# Basic Server Configuration
# ===========================================
# Name of your overlay node (lowercase, one word)
NODE_NAME=testnode

# Server port to listen on
PORT=8080

# BSV network to use (main or test)
NETWORK=test

# Authentication
# ===========================================
# Private key for node identity (REQUIRED)
# NOTE! : To make advertisement transactions it requires that the private key is used in the wallet toolbox
# Once the wallet tool box is running you can add a new private key here that contains funds.
SERVER_PRIVATE_KEY=804eecb436bc55a59f5beb6e9cc8f7715d6afc2afbd427c6ba924cec5aacf745

# Custom admin token for admin routes (optional - will be auto-generated if not provided)
ADMIN_TOKEN=

# Hosting Information
# ===========================================
# Domain name where your node is available, without https:// prefix (REQUIRED)
# Example: overlay.yourdomain.com
# 127.0.0.1:8080
HOSTING_URL=example.com

# Database Configuration
# ===========================================
# MySQL connection string (REQUIRED)
# Using MySQL instead of SQLite to support all migration syntax - seems SQLite is not compatible ?
KNEX_URL=mysql://root:password@localhost:3306/overlay_db

# MongoDB connection string (REQUIRED)
# Format: mongodb://username:password@hostname:port/database
MONGO_URL=mongodb://localhost:27017/overlay_db

# Integration Services
# ===========================================
# ARC API key for ARC integration (optional)
ARC_API_KEY=

# Sync Configuration
# ===========================================
# Enable GASP synchronization (true/false)
ENABLE_GASP_SYNC=true

# Logging Configuration
# ===========================================
# Enable verbose request logging (true/false)
VERBOSE_LOGGING=false
